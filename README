== Mod Manager ==

Mod Manager is being rewritten in PHP as of now running on top of SMF.

To install:
1. Copy over the contents of /Source to the root / of your SMF forum.
2. Open up phpMyAdmin or similar and import the database.sql file.
3. Run the file mbHooks.php with no parameters.

== mbHooks.php
mbHooks.php installs the required hooks for Mod Builder.
It acceps the following parameters:
- (no parameters) - Install the hooks.
- ?uninstall - Uninstall the hooks.
- ?reinstall - Uninstall and then install the hooks.
